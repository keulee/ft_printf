#include "ft_printf.h"

void    ft_printf_s(va_list *my_list)
{
    char    *str;
    //int     i;

    //i = 0;
    /*str = va_arg(*my_list, char *);
    while (str[i] != '\0')
    {
        if (i != 0 && (i - 1) == '%')

    }*/
        ft_putstr(str);
}
